console.log("ENGINE CARREGOU");

window.pararJogo = function() {
    if (window.gameLoop) {
        cancelAnimationFrame(window.gameLoop);
        window.gameLoop = null;
    }
}

window.pausarJogo = function(pausado) {
    window.jogoPausado = pausado;
}